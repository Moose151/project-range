# Copy to .env and fill in before deploying:  cp .env.example .env
#
# Required. Signs session cookies — MUST be a long random value in production.
# Generate one with:
#   python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=

# Optional overrides (defaults shown).
# Inactivity timeout for normal sessions, in minutes.
SESSION_TIMEOUT_MINUTES=480
# Max lifetime for "remember this terminal" sessions, in days.
SESSION_MAX_AGE_DAYS=30
# Per-file upload limit for package/CBM/CDA imports, in bytes.
MAX_UPLOAD_BYTES=2097152
# Combined upload payload limit for one import request, in bytes.
MAX_UPLOAD_TOTAL_BYTES=8388608
# Maximum files accepted inside a package import zip.
MAX_UPLOAD_ZIP_MEMBERS=100
